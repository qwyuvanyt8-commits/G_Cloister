{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm --prefix web run build && node -e \"require('fs').cpSync('web/.next', '.next', {recursive: true}) || true\"",
  "installCommand": "npm install --production=false"
}
